# piwigo-easy-config

解析默认配置文件并以可视化方式编辑设置。

Parse config_default and edit settings visually.

## 使用方法 Instructions for use
- 在 config.inc.php 中插入 `include_once('config_ec.inc.php');`
- Insert `include_once('config_ec.inc.php');` in config.inc.php